{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "edit": "allow",
    "bash": "allow",
    "webfetch": "allow",
    "external_directory": "deny"
  },
  "agent": {
    "build": { "prompt": "You are the build agent." },
    "plan": { "prompt": "You are the plan agent.", "permission": { "edit": "deny", "bash": "deny" } }
  }
}
