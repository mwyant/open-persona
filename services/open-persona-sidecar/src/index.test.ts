import { describe, expect, it } from "vitest";

// Basic sanity test to ensure the test runner is wired.
describe("open-persona-sidecar", () => {
  it("runs tests", () => {
    expect(true).toBe(true);
  });
});
